/*
 * Write a program in JAVA to create a class Calculator with methods for basic arithmetic operations
 * (addition, subtraction, multiplication, division).
 * 
 * Your program should:
 * 1. Create a Calculator class with four methods
 * 2. Implement addition, subtraction, multiplication, and division methods
 * 3. Each method should take two double parameters and return a double result
 * 4. Handle division by zero appropriately
 * 
 * IMPORTANT INSTRUCTIONS:
 * - Implement only the Calculator class and its methods
 * - Do NOT modify the main() method or the class structure
 * - Use appropriate data types for calculations
 * - Handle edge cases like division by zero
 */

import java.util.*;

/**
 * Calculator class with basic arithmetic operations
 */
class Calculator {
    
    /**
     * Method to add two numbers
     * @param a - First number
     * @param b - Second number
     * @return - Sum of a and b
     */
    public double addition(double a, double b) {
        return a + b;
    }
    
    /**
     * Method to subtract two numbers
     * @param a - First number
     * @param b - Second number
     * @return - Difference of a and b (a - b)
     */
    public double subtraction(double a, double b) {
        return a - b;
    }
    
    /**
     * Method to multiply two numbers
     * @param a - First number
     * @param b - Second number
     * @return - Product of a and b
     */
    public double multiplication(double a, double b) {
        return a * b;
    }
    
    /**
     * Method to divide two numbers
     * @param a - First number (dividend)
     * @param b - Second number (divisor)
     * @return - Quotient of a divided by b
     */
    public double division(double a, double b) {
        if (b == 0) {
            return 0.0; // Handle division by zero safely
        }
        return a / b;
    }
}

public class solution {
    
    /*************************************************************
     * DO NOT EDIT THE MAIN METHOD BELOW
     * THIS METHOD IS PROVIDED FOR INPUT/OUTPUT HANDLING
     * ONLY IMPLEMENT THE Calculator CLASS METHODS ABOVE
     *************************************************************/
    public static void main(String[] args) {
        double[][] input = {input};
        Calculator calc = new Calculator();
        
        double a = input[0][0];
        double b = input[0][1];
        String operation = String.valueOf((int)input[0][2]);
        
        double result = 0.0;
        switch(operation) {
            case "1":
                result = calc.addition(a, b);
                break;
            case "2":
                result = calc.subtraction(a, b);
                break;
            case "3":
                result = calc.multiplication(a, b);
                break;
            case "4":
                result = calc.division(a, b);
                break;
        }
        
        System.out.printf("%.2f", result);
    }
}
